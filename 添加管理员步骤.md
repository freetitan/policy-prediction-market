# 添加管理员账号 - 快速指南

## 邮箱：unimaster@gmail.com

---

## 步骤 1：执行辅助函数 SQL

1. 访问 Supabase: https://supabase.com/dashboard/project/peqmnkzptnrhfxqbpecq
2. 点击左侧 **"SQL Editor"**
3. 点击 **"New query"**
4. 打开项目文件 **`add-helper-functions.sql`**
5. 复制全部内容，粘贴到 SQL Editor
6. 点击 **"Run"** 执行

这将创建两个辅助函数：
- `get_user_by_email(email)` - 通过邮箱查找用户
- `get_user_email(user_id)` - 通过 ID 获取邮箱

---

## 步骤 2：添加自己为管理员

1. 在 SQL Editor 中点击 **"New query"**
2. 打开项目文件 **`add-admin.sql`**
3. 复制以下 SQL 代码：

```sql
-- 第一步：找到你的 user_id
SELECT 
    au.id,
    au.email,
    p.display_name
FROM auth.users au
LEFT JOIN profiles p ON au.id = p.id
WHERE au.email = 'unimaster@gmail.com';

-- 第二步：添加为管理员
INSERT INTO admins (user_id, username, role)
SELECT 
    au.id,
    COALESCE(p.display_name, 'Admin'),
    'super_admin'
FROM auth.users au
LEFT JOIN profiles p ON au.id = p.id
WHERE au.email = 'unimaster@gmail.com'
ON CONFLICT (user_id) DO NOTHING;

-- 第三步：验证是否添加成功
SELECT 
    a.id,
    a.username,
    a.role,
    a.active,
    au.email,
    p.display_name,
    a.created_at
FROM admins a
JOIN auth.users au ON a.user_id = au.id
LEFT JOIN profiles p ON a.user_id = p.id
WHERE au.email = 'unimaster@gmail.com';
```

4. 点击 **"Run"** 执行
5. 查看第三步的输出，确认显示你的管理员信息

**预期输出示例：**
```
id: xxxxx
username: Admin (或你的昵称)
role: super_admin
active: true
email: unimaster@gmail.com
display_name: xxx
created_at: 2026-06-03...
```

---

## 步骤 3：访问管理后台

1. 确保已登录账号（unimaster@gmail.com）
2. 访问：**https://policy-prediction-market.vercel.app/admin**
3. 如果成功，你将看到：
   - ✅ 管理员控制台界面
   - ✅ 统计面板
   - ✅ 三个管理标签页
4. 导航栏会显示"管理"链接（盾牌图标）

---

## 常见问题

### Q: 看不到管理链接？
**A:** 刷新页面或重新登录

### Q: 提示"访问被拒绝"？
**A:** 检查：
1. SQL 是否执行成功
2. `admins` 表中 `active` 是否为 `true`
3. 邮箱是否正确

### Q: SQL 执行出错？
**A:** 确保：
1. 先执行 `add-helper-functions.sql`
2. 再执行 `add-admin.sql`
3. 已经注册并登录过账号

---

## 所有 SQL 文件说明

| 文件名 | 用途 | 执行顺序 |
|--------|------|---------|
| `add-helper-functions.sql` | 创建辅助函数 | 第 1 步 |
| `add-admin.sql` | 添加管理员 | 第 2 步 |
| `admin-operations.sql` | 常用管理操作参考 | 可选 |

---

## 完成！🎉

现在你可以：
- ✅ 创建、编辑、结算市场
- ✅ 添加和管理验证者
- ✅ 查看和管理用户
- ✅ 调整用户积分

**管理后台地址：**
https://policy-prediction-market.vercel.app/admin
