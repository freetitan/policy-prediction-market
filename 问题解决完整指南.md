# 🛠️ 问题解决完整指南

## 问题1：登录按钮显示问题 ✅ 已修复

### 症状
- 登录按钮显示为一团阴影
- 用户菜单按钮不清楚
- 退出登录按钮看不清

### 解决方案
已修复并部署，刷新页面即可看到改进：
- ✅ 用户菜单按钮现在有明显的边框
- ✅ 下拉菜单背景色正确显示
- ✅ 退出登录按钮有清晰的hover效果

---

## 问题2：管理员权限问题 🔧 需要你操作

### 症状
访问 `/admin` 显示"访问被拒绝"

### 根本原因
数据库中没有 `admins` 表，或者你的账号不是管理员

### 解决步骤

#### 步骤 1：先注册账号（如果还没注册）

1. 访问：https://policy-prediction-market.vercel.app
2. 点击"注册"
3. 使用邮箱 `unimaster@gmail.com` 注册
4. 完成邮箱验证（检查邮件）
5. 登录账号

#### 步骤 2：在 Supabase 执行 SQL

1. 访问 Supabase: https://supabase.com/dashboard/project/peqmnkzptnrhfxqbpecq
2. 点击左侧 **"SQL Editor"**
3. 点击 **"New query"**
4. 复制下面的完整 SQL（选择方案 A 或 B）

---

### 🎯 方案 A：完整设置（推荐）

如果你不确定是否执行过设置，用这个方案最保险：

```sql
-- ==========================================
-- 完整设置（一次性执行）
-- ==========================================

-- 1. 创建管理员表
CREATE TABLE IF NOT EXISTS admins (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID UNIQUE NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  username TEXT NOT NULL,
  role TEXT NOT NULL DEFAULT 'super_admin',
  active BOOLEAN DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  last_login TIMESTAMPTZ
);

-- 2. 创建验证者表
CREATE TABLE IF NOT EXISTS verifiers (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID UNIQUE NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  reputation INTEGER DEFAULT 100,
  active BOOLEAN DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 3. 创建验证投票表
CREATE TABLE IF NOT EXISTS verification_votes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  market_id UUID NOT NULL REFERENCES markets(id) ON DELETE CASCADE,
  verifier_id UUID NOT NULL REFERENCES verifiers(id) ON DELETE CASCADE,
  vote BOOLEAN NOT NULL,
  evidence TEXT,
  confidence INTEGER DEFAULT 3,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(market_id, verifier_id)
);

-- 4. 创建管理员日志表
CREATE TABLE IF NOT EXISTS admin_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  admin_id UUID NOT NULL REFERENCES admins(id) ON DELETE CASCADE,
  action TEXT NOT NULL,
  details JSONB,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 5. 创建辅助函数
CREATE OR REPLACE FUNCTION get_user_email(user_id UUID)
RETURNS TEXT AS $$
DECLARE user_email TEXT;
BEGIN
    SELECT email INTO user_email FROM auth.users WHERE id = user_id;
    RETURN user_email;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

-- 6. 创建结算函数
CREATE OR REPLACE FUNCTION settle_market(p_market_id UUID, p_outcome BOOLEAN)
RETURNS VOID AS $$
DECLARE
  v_yes_pool INTEGER;
  v_no_pool INTEGER;
  v_total_pool INTEGER;
  v_winning_pool INTEGER;
BEGIN
  SELECT yes_pool, no_pool INTO v_yes_pool, v_no_pool FROM markets WHERE id = p_market_id;
  IF NOT FOUND THEN RAISE EXCEPTION '市场不存在'; END IF;
  
  v_total_pool := v_yes_pool + v_no_pool;
  IF p_outcome THEN v_winning_pool := v_yes_pool; ELSE v_winning_pool := v_no_pool; END IF;
  
  IF v_winning_pool > 0 THEN
    UPDATE profiles p
    SET points = points + FLOOR((b.amount::NUMERIC / v_winning_pool::NUMERIC) * v_total_pool::NUMERIC)::INTEGER
    FROM bets b WHERE b.user_id = p.id AND b.market_id = p_market_id AND b.position = p_outcome;
  END IF;
  
  UPDATE markets SET resolved = true, outcome = p_outcome, updated_at = NOW() WHERE id = p_market_id;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

-- 7. 启用安全策略
ALTER TABLE admins ENABLE ROW LEVEL SECURITY;
ALTER TABLE verifiers ENABLE ROW LEVEL SECURITY;
ALTER TABLE verification_votes ENABLE ROW LEVEL SECURITY;
ALTER TABLE admin_logs ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS "管理员可查看" ON admins;
CREATE POLICY "管理员可查看" ON admins FOR SELECT USING (
    EXISTS (SELECT 1 FROM admins WHERE user_id = auth.uid() AND active = true)
);

DROP POLICY IF EXISTS "任何人可查看验证者" ON verifiers;
CREATE POLICY "任何人可查看验证者" ON verifiers FOR SELECT USING (true);

-- 8. 添加 unimaster@gmail.com 为管理员
INSERT INTO admins (user_id, username, role, active)
SELECT 
    au.id,
    COALESCE(p.display_name, 'Admin'),
    'super_admin',
    true
FROM auth.users au
LEFT JOIN profiles p ON au.id = p.id
WHERE au.email = 'unimaster@gmail.com'
ON CONFLICT (user_id) 
DO UPDATE SET active = true, role = 'super_admin';

-- 9. 验证结果
SELECT 
    '✅ 设置完成！以下是你的管理员信息：' as 状态,
    au.email as 邮箱,
    a.username as 用户名,
    a.role as 角色,
    a.active as 激活状态,
    a.created_at as 添加时间
FROM admins a
JOIN auth.users au ON a.user_id = au.id
WHERE au.email = 'unimaster@gmail.com';
```

---

### 🚀 方案 B：快速添加（如果表已存在）

如果你已经执行过表创建，只需要添加管理员：

```sql
-- 添加管理员
INSERT INTO admins (user_id, username, role, active)
SELECT 
    au.id,
    COALESCE(p.display_name, 'Admin'),
    'super_admin',
    true
FROM auth.users au
LEFT JOIN profiles p ON au.id = p.id
WHERE au.email = 'unimaster@gmail.com'
ON CONFLICT (user_id) 
DO UPDATE SET active = true, role = 'super_admin';

-- 验证
SELECT 
    au.email,
    a.username,
    a.role,
    a.active
FROM admins a
JOIN auth.users au ON a.user_id = au.id
WHERE au.email = 'unimaster@gmail.com';
```

---

## 执行后的预期结果

### ✅ 成功的标志

执行 SQL 后，你应该看到类似这样的输出：

```
✅ 设置完成！以下是你的管理员信息：
邮箱: unimaster@gmail.com
用户名: Admin (或你的昵称)
角色: super_admin
激活状态: true
添加时间: 2026-06-03 ...
```

### ❌ 如果出错

**错误1：`relation "admins" already exists`**
- 解决：这是正常的，说明表已存在，直接用方案 B

**错误2：`no rows returned`**  
- 原因：邮箱 `unimaster@gmail.com` 还没注册
- 解决：先在网站注册账号，然后再执行 SQL

**错误3：`insert or update on table "admins" violates foreign key constraint`**
- 原因：auth.users 表中没有这个用户
- 解决：确认你已经在网站注册了 unimaster@gmail.com

---

## 步骤 3：访问管理后台

SQL 执行成功后：

1. **重新登录网站**（重要！）
   - 退出当前账号
   - 用 `unimaster@gmail.com` 重新登录

2. **访问管理后台**
   - URL: https://policy-prediction-market.vercel.app/admin
   
3. **如果还是不行**
   - 清除浏览器缓存（Ctrl+Shift+Delete）
   - 关闭浏览器完全重新打开
   - 尝试无痕模式

---

## 验证管理员权限

### 检查导航栏
登录后，导航栏应该显示：
- ✅ "管理"链接（带盾牌图标 🛡️）

### 检查管理后台
访问 `/admin` 应该看到：
- ✅ 统计面板（4个数据卡片）
- ✅ 三个管理标签页
- ✅ 欢迎信息显示你的用户名

---

## 🔍 诊断工具

如果还有问题，执行诊断SQL：

```sql
-- 检查你的账号是否存在
SELECT id, email, created_at 
FROM auth.users 
WHERE email = 'unimaster@gmail.com';

-- 检查 admins 表
SELECT COUNT(*) FROM admins;

-- 检查你是否是管理员
SELECT 
    a.*,
    au.email
FROM admins a
JOIN auth.users au ON a.user_id = au.id
WHERE au.email = 'unimaster@gmail.com';
```

把结果发给我，我可以帮你进一步诊断。

---

## 📁 项目中的SQL文件

所有SQL文件都在项目根目录：

| 文件名 | 用途 | 推荐指数 |
|--------|------|---------|
| `终极解决方案.sql` | 分步执行的完整方案 | ⭐⭐⭐⭐⭐ |
| `一键设置管理员.sql` | 一次性完整设置 | ⭐⭐⭐⭐⭐ |
| `诊断问题.sql` | 检查问题所在 | ⭐⭐⭐⭐ |
| `查找并添加管理员.sql` | 查找用户并添加 | ⭐⭐⭐ |
| `添加最新用户为管理员.sql` | 快速添加 | ⭐⭐⭐ |

---

## 🎉 成功后你可以做什么

进入管理后台后，你可以：

### 市场管理
- ✅ 创建新市场
- ✅ 编辑市场信息
- ✅ 手动结算市场
- ✅ 查看市场统计

### 验证者管理
- ✅ 添加验证者
- ✅ 停用/激活验证者
- ✅ 查看声誉分数

### 用户管理
- ✅ 查看所有用户
- ✅ 调整用户积分
- ✅ 查看投注记录
- ✅ 搜索和排序用户

---

## 🔗 相关链接

- **网站首页**: https://policy-prediction-market.vercel.app
- **管理后台**: https://policy-prediction-market.vercel.app/admin
- **Supabase**: https://supabase.com/dashboard/project/peqmnkzptnrhfxqbpecq
- **GitHub**: https://github.com/freetitan/policy-prediction-market

---

**如果还有问题，请把 SQL 执行的结果发给我！** 🚀
