# 管理员后台使用指南

## 一、添加管理员账号

### 方法 1：使用 SQL 脚本（推荐）

1. 登录到 Supabase Dashboard: https://supabase.com/dashboard
2. 选择你的项目（peqmnkzptnrhfxqbpecq）
3. 点击左侧菜单的 "SQL Editor"
4. 打开项目根目录的 `add-admin.sql` 文件
5. 将所有出现的 `'your-email@example.com'` 替换为你的实际邮箱地址
6. 复制修改后的 SQL 代码，粘贴到 SQL Editor 中
7. 点击 "Run" 执行
8. 检查输出，确认添加成功

### 方法 2：直接在 Supabase Table Editor 中添加

1. 登录 Supabase Dashboard
2. 点击 "Table Editor" → 选择 `profiles` 表
3. 找到你的用户记录，复制 `id`（这是 user_id）
4. 点击 "Table Editor" → 选择 `admins` 表
5. 点击 "Insert row"
6. 填写：
   - `user_id`: 你复制的用户 ID
   - `username`: 任意名称（例如：Admin）
   - `role`: 选择 `super_admin`
   - `active`: 勾选（true）
   - 其他字段自动填充
7. 点击 "Save"

## 二、访问管理员后台

1. 确保你已经登录账号
2. 访问：https://policy-prediction-market.vercel.app/admin
3. 如果你有管理员权限，将看到管理员控制台
4. 如果看到"访问被拒绝"，请检查：
   - 是否正确添加为管理员
   - `admins` 表中的 `active` 字段是否为 `true`
   - 刷新页面或重新登录

## 三、功能说明

### 3.1 统计面板

顶部显示四个关键指标：
- **总市场数**：平台所有市场的数量
- **活跃市场**：未结算的市场数量
- **总用户数**：注册用户总数
- **总积分池**：所有用户的积分总和

### 3.2 市场管理 (Markets Management)

#### 查看市场
- **全部**：显示所有市场
- **活跃**：仅显示未结算的市场
- **已结算**：仅显示已结算的市场

#### 创建市场
1. 点击"创建市场"按钮
2. 填写表单：
   - **市场标题**：简洁明确的问题（例如：2026年碳税税率是否会提高？）
   - **市场描述**：详细说明判定标准和规则
   - **分类**：选择合适的政策类别
   - **结束日期**：市场的截止日期
3. 点击"创建市场"

#### 编辑市场
- 点击市场卡片下方的"编辑"按钮
- 修改市场信息（仅限未结算的市场）
- 保存更改

#### 手动结算市场
- 点击"结算为'是'"或"结算为'否'"按钮
- 确认操作（此操作不可撤销）
- 系统自动：
  - 将市场标记为已结算
  - 计算并分配奖励给正确投注者
  - 根据总投注池按比例分配

**注意**：
- 只能结算未结算的市场
- 结算后无法修改结果
- 确保根据实际情况准确结算

### 3.3 验证者管理 (Verifiers Management)

#### 添加验证者
1. 点击"添加验证者"按钮
2. 输入用户的邮箱地址
3. 点击"搜索"查找用户
4. 确认用户信息后，点击"添加为验证者"

#### 管理验证者状态
- **停用**：点击验证者卡片的"停用"按钮
  - 停用后该用户无法参与市场验证
  - 不会删除历史验证记录
- **激活**：点击已停用验证者的"激活"按钮
  - 激活后该用户恢复验证权限

#### 声誉系统
- 每个验证者有声誉分数
- 正确验证：+10 分
- 错误验证：-5 分
- 声誉不影响投票权重，仅供参考

### 3.4 用户管理 (Users Management)

#### 查看用户列表
- **搜索**：通过昵称或邮箱搜索用户
- **排序**：
  - 按积分排序：显示积分排名
  - 按注册时间排序：显示注册顺序

#### 调整用户积分
1. 点击用户卡片的"调整积分"按钮
2. 选择操作类型：
   - **增加积分**：例如奖励活跃用户
   - **减少积分**：例如处罚违规行为
   - **设置为指定值**：直接设置积分数量
3. 输入积分数量
4. 填写原因（可选，但建议填写以便日后查询）
5. 预览变化后，点击"确认调整"

#### 查看用户投注记录
1. 点击"查看投注"按钮
2. 查看该用户的所有投注：
   - 投注的市场
   - 投注方向（是/否）
   - 投注金额
   - 投注时间
   - 胜负结果（如已结算）

## 四、操作日志

所有管理员操作都会自动记录在 `admin_logs` 表中，包括：
- 操作类型（创建市场、结算市场、调整积分等）
- 操作时间
- 操作详情
- 管理员 ID

可以在 Supabase Table Editor 中查看 `admin_logs` 表。

## 五、最佳实践

### 市场管理
1. **创建市场时**：
   - 确保问题清晰、可验证
   - 详细说明判定标准
   - 设置合理的结束日期
   - 选择正确的分类

2. **结算市场时**：
   - 确保有可靠的证据支持
   - 优先使用验证者投票系统
   - 仅在必要时手动结算
   - 双重确认结果无误

### 验证者管理
1. 选择可信赖、有相关领域知识的用户
2. 保持至少 3-5 个活跃验证者
3. 定期检查验证者的声誉分数
4. 对长期不活跃的验证者考虑停用

### 用户管理
1. **调整积分前**：
   - 确认操作原因
   - 记录详细说明
   - 避免随意调整

2. **常见调整场景**：
   - 系统错误导致积分异常
   - 活动奖励
   - 处理违规行为
   - 测试功能（在测试环境）

## 六、权限说明

### 角色类型
- **super_admin**：超级管理员
  - 拥有所有权限
  - 可以管理其他管理员（未来功能）
  
- **admin**：普通管理员（未来功能）
  - 可以管理市场、用户、验证者
  - 不能管理其他管理员

### 安全建议
1. 不要将管理员密码分享给他人
2. 定期更换密码
3. 谨慎操作，特别是结算和积分调整
4. 操作前仔细确认
5. 记录重要操作的原因

## 七、常见问题

### Q1: 看不到"管理"链接？
A: 检查：
- 是否已添加为管理员
- `admins` 表中 `active` 是否为 `true`
- 是否已登录
- 刷新页面或重新登录

### Q2: 无法结算市场？
A: 确认：
- 市场是否已经结算
- 是否有系统错误（查看浏览器控制台）
- 检查 Supabase 中 `settle_market` 函数是否正常

### Q3: 如何撤销错误的结算？
A: 市场结算后无法自动撤销，需要：
1. 在 Supabase 中手动修改数据库
2. 恢复用户积分
3. 重新计算分配
4. **建议联系技术支持处理**

### Q4: 如何查看操作历史？
A: 登录 Supabase → Table Editor → 选择 `admin_logs` 表

### Q5: 如何删除管理员？
A: 在 Supabase Table Editor 中：
- 方式 1：将 `admins` 表中对应行的 `active` 设为 `false`
- 方式 2：直接删除该行

## 八、技术支持

如遇到问题：
1. 检查浏览器控制台错误信息
2. 查看 Supabase 日志
3. 检查数据库表结构和数据
4. 联系技术管理员

---

**重要提醒**：管理员权限强大，请谨慎使用。所有操作都有日志记录，请确保操作合理合规。
